const content=document.querySelector("#content");const title=document.querySelector("#title");let view="records";async function request(path){const response=await fetch(`http://localhost:8080${path}`);if(!response.ok)throw new Error(await response.text());return response.json()}function card(item){const node=document.createElement("article");node.className="item";node.innerHTML=`<h2>${item.name}</h2><div class="meta"><span>${item.category||"门店新品"}</span><span>¥${(item.price_cents/100).toFixed(2)}</span></div><div class="tags">${(item.tags||[]).join(" · ")}</div>`;return node}async function render(){title.textContent=view==="templates"?"可用配方库":view==="profile"?"顾客推荐":"新品资料";content.replaceChildren();try{const items=view==="templates"?await request("/templates"):[];if(!items.length){content.append(document.querySelector("#empty").content.cloneNode(true));return}items.slice(0,24).forEach(item=>content.append(card(item)))}catch(error){content.textContent=`服务暂不可用：${error.message}`}}document.querySelectorAll("nav button").forEach(button=>button.addEventListener("click",()=>{document.querySelector("nav .active").classList.remove("active");button.classList.add("active");view=button.dataset.view;render()}));document.querySelector("#refresh").addEventListener("click",render);request("/health").then(()=>document.querySelector("#health").textContent="服务在线").catch(()=>document.querySelector("#health").textContent="离线模式");render();
